"""Layers that were built, measured, and removed from the live stack.

Kept executable rather than deleted, for the same reason frozen architectures are: a retired
layer's numbers must stay reproducible, or the reason it was retired becomes a claim instead of
a measurement. Nothing here is registered in the contract and nothing here runs in `cortex.py`.

See `architecture-history/CORVUS_L2_CLUSTER.md`.
"""
