"""Mechanisms that were built, gated, and refuted.

Kept executable rather than deleted, for the same reason frozen architectures are: a retired
mechanism's numbers must stay reproducible, or the reason it was retired becomes a claim instead
of a measurement.

See `architecture-history/JAY_L1_BINDING.md`.
"""
