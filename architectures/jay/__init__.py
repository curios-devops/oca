"""OCA v5 — Jay.

Named for the corvids that cache food and recover it later: Eurasian and scrub jays are the
standard animal model for *what-where-when* memory, which is where this line is headed.

**What v5 changes, and it is exactly one thing.** Corvus froze at `v4.4-alpha` having cleared
every floor it declared, and having lost Gate B to its own sensory input by 0.178. The
aggregate-then-vote experiment then located the loss precisely: every Corvus-derived condition
scored 0.585-0.608 on object kind where the pixels scored 0.782, and **one tower already reached
that ceiling** -- nine towers, five-tower assemblies, voting and averaging all landed inside a
spread of 0.023. There was nothing left to aggregate. The information was gone before any higher
level could see it.

So v5 changes **Layer 1** and nothing else. Layer 0 is carried across because it passes at
+0.917 and because A-02 was re-measured on it rather than inherited. Layer 2 does not exist yet
and will not until Layer 1 clears its floor.
"""
